services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wedding-road-trip
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 5000
    ports:
      - "5001:5000"
    volumes:
      - ./data:/app/data
